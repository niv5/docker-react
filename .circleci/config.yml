jobs:
 build:
   machine: true
   steps:
    - checkout
    - run: docker build -t nivankovic/docker-react -f Dockerfile.dev .
    - run: docker run nivankovic/docker-react npm run test -- --coverage
